version: '3.3'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./database.sqlite:/app/database.sqlite
      - ./estimates.db:/app/estimates.db
    environment:
      - NODE_ENV=production
    restart: unless-stopped
